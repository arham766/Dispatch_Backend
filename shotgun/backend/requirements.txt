fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
httpx>=0.27
sse-starlette>=2.1
PyGithub>=2.3
asyncpg>=0.29
sqlalchemy[asyncio]>=2.0
aiokafka>=0.10
python-dotenv>=1.0

# Auth + crypto
firebase-admin>=6.5
PyJWT>=2.8
cryptography>=42.0
PyNaCl>=1.5

# Email
resend>=0.8

# Optional: gunicorn for prod (uvicorn alone is fine for free/starter tier)
# gunicorn>=21.2
